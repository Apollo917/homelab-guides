global:
  scrape_interval: 1m

scrape_configs:
  - job_name: dns-based
    scrape_interval: 30s
    dns_sd_configs:
      - names:
          - "node-one.example.com"
          - "node-two.example.com"
        type: A
        port: 9100
        refresh_interval: "10m"
