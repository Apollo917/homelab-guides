name: "vaultwarden"
services:
  vaultwarden:
    image: vaultwarden/server:1.36.0
    container_name: vaultwarden
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    env_file: .env
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./data/:/data/
    networks:
      - reverse-proxy
    labels:
      # Traefik integration labels - start
      - "traefik.enable=true"
      - "traefik.http.services.vaultwarden.loadbalancer.server.port=80"
      - "traefik.http.routers.vaultwarden.service=vaultwarden"
      - "traefik.http.routers.vaultwarden.tls=true"
      - "traefik.http.routers.vaultwarden.entrypoints=private-https"
      - "traefik.http.routers.vaultwarden.rule=Host(`${VAULTWARDEN_SERVICE_URL:?error}`)" # e.g. vaultwarden.example.com
      # Traefik integration labels - end
networks:
  reverse-proxy:
    external: true
